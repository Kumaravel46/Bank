name: 'bank_analytics'
version: '1.0.0'
config-version: 2

profile: 'bank_analytics'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]

models:
  bank_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: analytics
